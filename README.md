# Meta-Tsunami
A lightweight, C-based cybersecurity tool designed to anonymize JPEG files by surgically removing privacy-sensitive metadata segments (EXIF, XMP, IPTC, and COM) through low-level binary parsing.
